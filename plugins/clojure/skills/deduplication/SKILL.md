---
name: deduplication
description: Recognize and remove duplication in Clojure code — extract shared logic into helper functions, threading macros, or higher-order forms. Use when writing or reviewing Clojure code that contains repeated patterns.
---

# Clojure Deduplication

## When to Use

Apply this skill when writing or editing Clojure code that contains repeated patterns — duplicated expressions, parallel `let` bindings, repeated branches, or near-identical function bodies.

## Examples

<!-- Add examples below. -->
